export enum QuantityUnit {
  LITRE = "litre",
  PIECES = "pieces",
  PACKS = "packs",
}
export enum DeliveryOption {
  PICKUP = "pickup",
  DELIVERY = "delivery",
  BOTH = "both",
}
export enum Currency {
  NGN = "NGN",
}
